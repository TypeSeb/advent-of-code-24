import { readFileSync } from 'fs'
import path from 'path'

const lines = readFileSync(path.join(__dirname, '../../src/day-5', 'input.txt'), 'utf-8').split('\n')

export function second() {
  //console.log(input)
}
